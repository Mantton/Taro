{
  "name": "tree-sitter-taro",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for the Taro programming language",
  "keywords": [
    "parser",
    "tree-sitter",
    "taro"
  ],
  "devDependencies": {
    "tree-sitter-cli": "^0.24.0"
  },
  "scripts": {
    "build": "tree-sitter generate",
    "test": "npm run build && node --test test/editors.test.js && tree-sitter test",
    "prepare:zed": "npm run build && node ../editors/zed/prepare.js"
  },
  "private": true
}
