// EXPECT_EXIT: 101
// EXPECT_STDERR_CONTAINS: panic: attempt to divide with overflow
func div(_ a: int64, _ b: int64) -> int64 {
    return a / b
}

func main() {
    let min = (1 as int64) << 63
    let q = div(min, -1)
    assert(q == min, "unreachable: MIN / -1 should panic first")
}
