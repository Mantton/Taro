// Test: Dictionaries Basic
// Checks basic dictionary creation and insertion.

func main() {
    var a: [int32: string] = [:]
    var b: [uint32: string] = Dictionary()

    // Insert calls are enough to exercise dictionary plumbing.
    let c = a.insert(key: 10, value: "Hello, World")
    let d = b.insert(key: 10, value: "Hello, World")
    
    std.print("PASS: Dictionaries basic usage\n")
}
