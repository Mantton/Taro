import std.hash.{Hashable, Hasher}
import std.cmp.Equatable

extend rune: Hashable {
    public func hash[H: Hasher](&self, into hasher: &mut H) {}
    public operator ==(self, rhs: rune) -> bool { self == rhs }
    public operator !=(self, rhs: rune) -> bool { self != rhs }
}
