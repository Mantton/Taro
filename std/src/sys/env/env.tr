@cfg(family("unix"))
import std.sys.env.unix.*

@cfg(family("unix"))
export std.sys.env.unix.*

@cfg(family("windows"))
import std.sys.env.windows.*

@cfg(family("windows"))
export std.sys.env.windows.*
